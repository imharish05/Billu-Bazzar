<USER_REQUEST>
You are a full-stack e-commerce developer. Follow every instruction below exactly, in order. Skip nothing. Assume nothing unstated â€” if unclear, state assumption in plain text, then continue.

Build "Billu Bazaar" â€” luxury e-commerce site â€” from scratch, complete, working, production-polish, visually distinctive (not generic template look).

<tech_stack>
- Frontend (customer + admin, separate apps): React 18, Redux Toolkit, React Router v6, Tailwind CSS, shadcn/ui, lucide-react (icons)
- Backend: Node.js, Express.js, Sequelize ORM, MySQL (db: billu_bazaar, user: root, password: blank, host: localhost). Auto-sync via sequelize.sync({ alter: true }).
- Animation/UI â€” non-overlapping roles, do not blur:
  - Framer Motion â†’ modal open/close, cart drawer slide-in, page fade transitions (200ms), staggered list entrance on product grids
  - anime.js â†’ banner text entrance, dashboard counters, product card hover shimmer
  - Skiper UI (`npx shadcn add @skiper-ui/[component]`) â†’ carousel/marquee/theme-toggle
  - Vengeance UI (own CLI) â†’ hero displacement-hover, tooltips, scroll-reveal cards
  - React Bits (jsrepo CLI) â†’ text-effect components ONLY: gradient/shiny/split-text on Home hero title, Product Details price reveal, Dashboard section headers, Category page headers. Max 2-3 per page.
  - Animmaster Lib â†’ manual copy-paste, no CLI/npm. ONE use: Home page WebGL hero background. Comment: "// manual snippet from Animmaster â€” verify license before production use"
- Feature libraries: Typesense (search), currency.js (currency), @headlessui/react (modals), Google model-viewer + react-360-view (360/AR), Plyr.js (video), Nodemailer (email), Twilio (SMS/OTP/WhatsApp), Razorpay (payment), Shiprocket API (shipping), Mapbox GL JS (tracking map), Crisp (chat), Multer + Cloudinary (upload), Recharts (charts), PDFKit (invoices/labels), ExcelJS (export), node-cron (jobs), web-push (push)
- Third-party mocking: Typesense/Razorpay/Shiprocket/Mapbox/Twilio/Crisp/Cloudinary â†’ local mock classes /server/services, app runs fully out-of-box
</tech_stack>

<visual_design_direction>
Reject generic-template look. Follow explicitly:
- Asymmetric layouts on Home and Product Listing â€” not everything centered/boxed. Use offset grids, overlapping image+text blocks, varied card sizes (feature 1 large product card per row-of-4 on New Arrivals section).
- Typography scale: define explicit type ramp before coding â€” H1 48-64px, H2 32-40px, H3 24px, body 16px, caption 13px â€” Playfair Display headings at large sizes should feel editorial/magazine-like, not just bold text.
- Depth/texture: subtle gradient overlays on hero/banner images (dark-to-transparent gold-tinted gradient for text legibility), noise/grain texture option on hero background (5% opacity) for premium tactile feel â€” do not leave flat solid colors on large hero sections.
- Whitespace rhythm: hero sections get generous vertical padding (64px+ per polish_layer spacing scale), do not cram content â€” luxury brands breathe.
- Per-section animation variety â€” do NOT put all animation only on Home hero. Distribute across the site:
  - Home: hero stack (existing) + scroll-triggered fade-up on "New Arrivals" section + React Bits text-effect on section headers
  - Product Listing: Framer Motion staggered grid entrance (cards fade+slide up in sequence, 50ms stagger) + Vengeance UI scroll-reveal
  - Product Details: image gallery crossfade on thumbnail click (Framer Motion), tab-switch slide animation
  - Cart: line-item remove = shrink+fade exit animation (Framer Motion AnimatePresence), not instant disappear
  - Admin Dashboard: stat cards stagger-in on load (anime.js), chart draw-in animation on Recharts (built-in animation prop enabled)
  - Checkout: step-indicator progress animation between addressâ†’paymentâ†’confirm
- Every animation must have a clear purpose (guide attention, confirm action, show state change) â€” no animation for decoration alone. State the purpose in a one-line comment above each animation block.
</visual_design_direction>

<branding>
No logo file yet. Don't hardcode image. `<Logo />` component (both apps) renders brand text (Playfair Display, gold #C9A24B) via CSS var `--brand-logo-text: "Billu Bazaar"`. Placeholder favicon: gold "BB" monogram SVG. Swap both when real logo supplied.
</branding>

<polish_layer>
Every page, both apps, no skipping:
- Elevation: shadow-sm (cards), shadow-md (hover), shadow-lg (modals/dropdowns) â€” only these 3
- Icons: lucide-react only, 20px, 1.5px stroke
- Loading: skeleton pulse for every data-fetch component
- Empty states: icon + message + primary action button
- 404 page: both apps, Logo + gold message + "Back to Home"
- SEO/meta: dynamic title+description per /client page, min Home/Listing/Details
- A11y: WCAG AA contrast (4.5:1 body, 3:1 large text) â€” verify #1A1A1A/#FAFAF8 and #C9A24B pairings, flag if fail
- Hover/focus: every interactive element gets hover state + visible keyboard focus ring
- Smooth scroll site-wide, respect prefers-reduced-motion â€” disable decorative animation libs when set
</polish_layer>

<npm_install_commands>
Exact install per /client, /admin, /server. Separate step for Skiper UI, Vengeance UI, React Bits CLI commands. Include lucide-react, react-helmet.
</npm_install_commands>

<folder_structure>
/client, /admin, /server â€” no merging.
/client/src: components, pages, redux, redux/slices, services, assets, hooks
/admin/src: components, pages, redux, redux/slices, services
/server: controllers, routes, models, middleware, services, jobs, config, utils
Skiper UI / Vengeance UI / React Bits components â†’ /client/src/components/ui.
</folder_structure>

<responsive_design_rules>
Mobile <640px: 1-col, hamburger nav. Tablet 640â€“1023px: 2-col grid, top-4-category nav. Desktop 1024â€“1279px: 3-col grid, full nav. Large desktop â‰¥1280px: 4-col grid, max-width 1440px centered. State breakpoint behavior per page before coding.
</responsive_design_rules>

<design_system>
- Colors ONLY: #FAFAF8 bg, #1A1A1A text, #C9A24B gold, #FFFFFF white, #6B6B6B grey
- Fonts: Playfair Display (headings), Inter (body) â€” see visual_design_direction for type ramp
- Spacing ONLY: 8/16/24/32/48/64px
- Effect lanes: anime.js=counters/shimmer/banner-entrance/dashboard-stagger; Framer Motion=modals/drawer/transitions/grid-stagger/gallery-crossfade/exit-animations; Skiper UI=carousel/marquee/toggle; Vengeance UI=hero-hover/tooltips/scroll-reveal; React Bits=heading text-effects only; Animmaster=hero WebGL bg only
- Texture/gradient/asymmetry rules per visual_design_direction are part of design system, not optional
</design_system>

<database_schema>
18 Sequelize models /server/models: AdminUser, Role, Customer, Product, Category, Vendor, Warehouse, WarehouseStock, Cart, CartItem, Order, OrderItem, Wishlist, Coupon, LoyaltyLedger, Affiliate, SupportTicket, Banner.

Key relations:
- AdminUser belongsTo Role, Role hasMany AdminUser (Role=ADMIN/STAFF only)
- Category belongsTo Category (ParentCategory/parentId), hasMany Category (SubCategories), attributes JSON
- Product belongsTo Category/Vendor, belongsToMany Warehouse thru WarehouseStock, hasMany CartItem/OrderItem/Wishlist
- Product 360/AR: spin_images JSON, model_3d_url STRING nullable
- Customer hasOne Cart, hasMany Order/Wishlist/LoyaltyLedger/SupportTicket
- Order belongsTo Customer/Affiliate/Coupon, hasMany OrderItem

/server/models/index.js = association loader.
</database_schema>

<admin_pages_exact_list>
14 pages: Dashboard (revenue chart w/ draw-in animation, 3 stagger-in stat cards w/ anime.js counters, recent orders, low-stock alerts, segmentation widget, skeletons), Banners (table+modal CRUD, drag-reorder, empty state), Categories (tree view, attributes JSON form), Products (table+form incl. 360 spin frames+3D model upload), Vendors (CRUD), Warehouses (CRUD), Orders (table+detail, invoice/label PDFKit, fraud flag), Payments & Transactions (table, filter, Excel export, gateway/shipping status), Coupons (CRUD), Customers (table+detail), Loyalty (settings+ledger), Affiliates (CRUD), Reports (date picker, sales/best-seller/behavior/abandoned-cart/push-perf tables, Excel export), Settings (content mgmt, shipping config, coupon config, role mgmt, marketing automation).

Every table: skeleton, empty state, row hover.
</admin_pages_exact_list>

<customer_pages_exact_list>
7 pages: Home (hero stack + asymmetric New Arrivals section w/ scroll-fade-up + React Bits header text + category nav + Typesense search + curated carousel + influencer showcase + currency toggle), Product Listing (asymmetric grid w/ Framer Motion staggered entrance + filter/sort/pagination + quick-view + wishlist + Vengeance scroll-reveal), Product Details (gallery w/ crossfade + 360/AR + video tab + tab-switch animation + related carousel + Add to Cart/Buy Now + Notify Me + trust badges + React Bits price reveal), Cart (line items w/ shrink-fade exit animation + gift wrap + coupon + Framer Motion drawer + empty state), Checkout (address/Mapbox + payment/Razorpay + delivery estimate + OTP + animated step-indicator progress + summary), Order Confirmation (thank-you + email/SMS + PDFKit invoice + tracking map + WhatsApp opt-in), Account (tabs: Profile/Orders/Wishlist/Loyalty/Personal Shopper/Support).

404 required both apps.
</customer_pages_exact_list>

<constraints>
- Implement every item in admin/customer lists + polish_layer + visual_design_direction. No silent omissions; flag if infeasible.
- Follow /client, /admin, /server split exactly.
- Only libraries named, no swap without flagging.
- No invented requirements. Ambiguous â†’ state assumption.
- Unsure lib capability â†’ [uncertain â€” verify before use].
- Verify WCAG contrast before finalizing CSS.
- Max 2-3 React Bits components per page.
- Every animation needs one-line purpose comment â€” no decoration-only motion.
</constraints>

<output_instructions>
Step 1: npm install + Skiper/Vengeance/React Bits CLI commands
Step 2: Tailwind config + CSS vars (colors/fonts/spacing/shadows/type-ramp) + Logo/favicon + WCAG confirmation
Step 3: Sequelize models one at a time + index.js
Step 4: Mock service classes /server/services
Step 5: Server controllers+routes one at a time (models + auth/RBAC middleware)
Step 6: Client pages one at a time, listed order, incl. skeleton/empty/SEO/404/per-section animations
Step 7: Admin pages one at a time, listed order, incl. skeleton/empty/404/dashboard animations
Step 8: After each step: "âœ… Step [n] complete â€” files: [list]"
Step 9: Final checklist: Featureâ†’File, all 21 pages+404s, animation-lib-per-element incl. purpose, 360/AR flow, Category subtree+attributes, Payments page, polish-layer + visual-design items confirmed per page
</output_instructions>

<rules>
- No placeholder/"implement similarly" â€” full real code always
- No skip/merge steps
- Unsure lib function â†’ "[uncertain â€” verify before use]", continue anyway
- Long step â†’ stop at file boundary, "Reply 'continue' for next file", wait
- Animmaster snippet isolated, license-verify comment
- No hardcoded logo image â€” <Logo /> text-component + CSS var only
- Respect prefers-reduced-motion â€” disable decorative libs when set
- Reject flat/generic template look â€” asymmetry, texture, editorial type scale mandatory
</rules>
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-07-07T17:14:52+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.5 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
